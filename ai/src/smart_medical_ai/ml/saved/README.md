# Saved Models

Trained model artifacts are written here by:

```bash
PYTHONPATH=src python -m smart_medical_ai.ml.train
```

The API also auto-generates a baseline model here on first startup if no model exists yet.
